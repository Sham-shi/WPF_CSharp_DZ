<Window
    x:Class="View.ToDo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodel="clr-namespace:ViewModel;assembly=ViewModel"
    Title="ToDo"
    Width="800"
    Height="450"
    d:DataContext="{d:DesignInstance Type=viewmodel:ToDoViewModel}"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="100" />
        </Grid.RowDefinitions>
        <ListBox Grid.Row="0" ItemsSource="{Binding Tasks}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox
                            Margin="5"
                            Command="{Binding DataContext.ToggleTaskCompletionCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                            CommandParameter="{Binding}"
                            IsChecked="{Binding IsComplete, Mode=TwoWay}" />

                        <TextBlock
                            Width="300"
                            VerticalAlignment="Center"
                            Text="{Binding Name}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="1">
            <StackPanel Orientation="Horizontal">
                <Label Content="Название" />
                <TextBox
                    Width="350"
                    Height="40"
                    Text="{Binding TextTask}" />
            </StackPanel>
            <Button Command="{Binding AddTaskCommand}" Content="Добавить" />
            <Button Command="{Binding SaveTasksToFileCommand}" Content="Посмотреть" />
        </StackPanel>
    </Grid>
</Window>
